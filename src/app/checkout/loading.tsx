import { StorefrontLoadingShell } from '@/components/ui/loading-shells'

export default function Loading() {
  return (
    <StorefrontLoadingShell
      title="Preparando o checkout"
      description="Estamos carregando seus dados para deixar a finalizacao do pedido mais rapida."
    />
  )
}
