import { AppLoadingScreen } from '@/components/ui/loading-shells'

export default function Loading() {
  return (
    <AppLoadingScreen
      title="Carregando a aplicacao"
      description="Estamos preparando a pagina e buscando as informacoes mais recentes para voce."
    />
  )
}
