import { StorefrontLoadingShell } from '@/components/ui/loading-shells'

export default function Loading() {
  return (
    <StorefrontLoadingShell
      title="Carregando o carrinho"
      description="Estamos revisando os itens adicionados e preparando o resumo da sua compra."
    />
  )
}
