import { StorefrontLoadingShell } from '@/components/ui/loading-shells'

export default function Loading() {
  return (
    <StorefrontLoadingShell
      title="Carregando sua conta"
      description="Estamos separando seus dados, pedidos e preferencias para montar a area do cliente."
    />
  )
}
