import { StorefrontLoadingShell } from '@/components/ui/loading-shells'

export default function Loading() {
  return (
    <StorefrontLoadingShell
      title="Carregando a categoria"
      description="Estamos organizando os produtos e filtros desta categoria para voce navegar melhor."
    />
  )
}
